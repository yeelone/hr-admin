import { CanAccessDirective } from './can-access.directive';

describe('CanAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new CanAccessDirective();
    expect(directive).toBeTruthy();
  });
});
